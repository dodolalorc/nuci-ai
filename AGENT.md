# Nuci AI 协作指南

Nuci AI 是一个本地优先的浏览器扩展：帮助用户从网页中采集内容、获得 AI 整理结果，并把可检索的知识条目保存到本地。产品的唯一主线是“采集 → 整理 → 回看”；书签归档是可选能力，不能反过来主导产品。

## 开始前必读

1. 阅读 `.agents/product-plan.md`，确认本次改动对应的阶段与验收标准。
2. 阅读 `.agents/code-review.md`，并把其中的阻断项作为合并门槛。
3. 先执行 `git status --short`，保留用户已有的未提交改动。
4. 修改前用 `rg` 查找同类实现，优先收敛或复用，不新增平行逻辑。

## 架构边界

- `src/contents/`：网页 DOM 读取、选区与页面内交互。不得直接写 `chrome.storage` 或调用 AI。
- `src/background.ts`：仅做消息路由和权限边界；复杂业务必须下沉至 service。
- `src/services/`：采集、AI、知识条目等领域能力；每项能力只有一个权威实现。
- `src/sdk/`：浏览器 API、存储、消息协议和通用工具。跨入口数据模型与存储键只在此处定义。
- `src/views/`、`src/content-ui/`：展示与局部交互。组件不应同时承担数据迁移、AI 调用和跨页面编排。
- `src/types/`：领域模型。不得为同一实体维护两份不兼容的类型。

## 必须遵守的实现规则

- 新增消息先在统一的消息类型/客户端中定义 request 与 response；不得散落字符串字面量和 `unknown` 强制断言。
- 新增或调整存储结构必须提供版本号和迁移策略；导入、导出、读取都要兼容旧数据。
- “保存网页”必须具备幂等策略（同一 URL 的更新/重复保存决策明确）和可感知的失败状态。
- API Key 只保存在扩展本地存储；禁止记录到日志、导出到普通调试信息或渲染到页面。
- AI 输出是非可信输入：解析、字段校验、长度限制与失败降级缺一不可。
- 内容脚本要兼容无正文、动态页面、iframe、受限页面与超长文本；DOM 变更必须可清理，不能污染宿主网页布局。
- 只在用户确认后写入或移动浏览器书签；批量操作必须提供预览与可撤销记录。

## 改动流程

1. 说明目标、影响入口、数据变更和回滚方式。
2. 以小步提交实现；大文件先拆分职责，再添加功能。
3. 至少执行 `pnpm typecheck`；可用时再执行 `pnpm build`。
4. 人工验证受影响流程：未配置模型、AI 失败、空正文、重复保存、刷新页面和导入旧备份。
5. 在交付说明中列出行为变化、迁移影响、验证命令及未验证项。

## 当前优先级

按 `.agents/product-plan.md` 的 Phase 0 → 3 顺序推进。未经明确产品决策，不要扩展新入口、新存储实体或新 AI 能力。
